package database;

/**
 * This Class handles all incoming json packets and adds the relevant data to the DB.
 * It also makes sure that the json packets don't contain sql code injection request
 * and if they do it handles them gracefully
 */

public class DBJsonHandler {
    //TODO:
    public DBJsonHandler(){}
}
